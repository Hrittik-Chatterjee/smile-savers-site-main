# Design-System Semantic Model — Gap Audit (READ-ONLY)

Produced exactly as the requesting spec's §28 asked: a read-only audit before any implementation, stopping at the implementation boundary. **Nothing in `design-intelligence/semantic/` or elsewhere has been created as a result of this document.**

## Governing finding

The 33-section request models a design system with React/TSX components (`src/components/Button.tsx`), a co-located test suite (`tests/button.spec.ts`), formal anatomy/pattern/anti-pattern documentation, and design-intent rationale. **None of that exists in this repository.** The site is 47 Astro (`.astro`, not `.tsx`) components, zero test files of any kind, no Storybook or component catalog, no pattern-library documentation, and no recorded design rationale beyond what's inferable from the code itself.

This matters because §31 of the request itself says: _"Never fill gaps with AI assumptions. A semantic model that admits uncertainty is better than one that invents relationships."_ Building the requested 12-domain semantic graph (anatomy, composition, states, motion, responsive, accessibility, patterns, anti-patterns, design intent) for this repository would mean **inventing** the ~90% of that content that was never documented by whoever built the site — which is exactly what §31 forbids. The honest gap-audit answer is that most of the requested model has no evidence to build from, not that it's merely unbuilt.

## §28's 10 read-only questions, answered with evidence

| #   | Question                                              | Answer                                                                                                                                                                                                                                                                                                                                                                                                   | Evidence                                                                                                                       |
| --- | ----------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------ |
| 1   | What semantic capabilities already exist?             | A semantic **token** layer exists, partially. `src/styles/global.css` defines named custom properties (`--color-primary`, `--spacing-section`, `--radius-lg`) rather than only raw literals — this is real semantic intent, not just values.                                                                                                                                                             | `src/styles/global.css:24,49,54,59,63,89,200-254`                                                                              |
| 2   | What relationships already exist?                     | None as a queryable model. Components reference tokens via `var(--x)` in scoped `<style>` blocks; this is a real relationship but exists only as unindexed CSS, not as data.                                                                                                                                                                                                                             | grep across `src/components/**/*.astro`, `src/modules/**/*.astro`                                                              |
| 3   | What does the current token model know?               | The **design-intelligence** pipeline (Stages 0-20, separate subsystem) built a full 6-layer token model — but for the Google Labs reference system, not for Smile Savers' own tokens. Smile Savers' own tokens exist only as CSS custom properties with no id/layer/status/consumer metadata.                                                                                                            | `design-intelligence/artifacts/tokens/reference-tokens.json` (Labs); no equivalent exists for `src/styles/global.css`          |
| 4   | What component model exists?                          | Informal only. Only 5 of 47 components (`Button.astro`, `BeforeAfter.astro`, `CTASection.astro`, `PageHeader.astro`, `Navigation.astro`) declare a typed `interface Props`. The other 42 have no formal contract — no documented variants, no documented slots, no documented states beyond what's implicit in scoped CSS.                                                                               | `grep -l "interface Props" src/components/**/*.astro src/modules/**/*.astro` → 5/47                                            |
| 5   | What responsive/state/motion models exist?            | For Smile Savers: none, formally. `Button.astro` is the one genuine exception — it has a real, extractable state model (`:hover`, `:active`, `:focus-visible`, `[disabled]`) and a real reduced-motion rule. For Labs (the external reference system): a full state/motion/responsive model was built in Stages 4-6 of the design-intelligence pipeline, but that describes Google's site, not this one. | `src/components/ui/Button.astro` (read in full this turn); `design-intelligence/artifacts/evidence/states/`, `.../responsive/` |
| 6   | What evidence lineage exists?                         | Full evidence-graph infrastructure exists (`design-intelligence/lib/cache.mjs`, `lib/evidence.mjs`, `scripts/graph.mjs`) but it is scoped to the Labs extraction. Nothing in this repo currently produces evidence records _about Smile Savers' own components_.                                                                                                                                         | `design-intelligence/artifacts/graph/evidence-graph.json` (Labs-scoped)                                                        |
| 7   | What impact analysis exists?                          | None. No change-impact engine of any kind exists for either Labs tokens or Smile Savers components.                                                                                                                                                                                                                                                                                                      | —                                                                                                                              |
| 8   | What context routing exists?                          | One policy document (`design-intelligence/knowledge/policies/context-routing.json`), added last turn as documentation of intent — explicitly not a runtime "context compiler."                                                                                                                                                                                                                           | `design-intelligence/knowledge/policies/context-routing.json`                                                                  |
| 9   | What is missing (relative to the 33-section request)? | Nearly all of it, for Smile Savers specifically: anatomy model, composition model, pattern/anti-pattern taxonomy, design-intent records, accessibility-to-component linkage, change-impact engine, evidence-invalidation graph, agent-neutral core + multi-agent adapters.                                                                                                                               | —                                                                                                                              |
| 10  | What can be reused?                                   | The evidence-class vocabulary (`OBSERVED-SOURCE`/`DERIVED`/`MAPPED`/etc.), the content-addressed cache, the evidence-record schema, and the relationship-taxonomy _discipline_ (typed edges, not free text) from the Labs pipeline are directly reusable patterns — the infrastructure is sound, only the Smile-Savers-specific content is missing.                                                      | `design-intelligence/lib/`, `design-intelligence/schemas/`                                                                     |

## Classified gaps

| Item                                                | Current                                                                                                                                               | Required (per request)                                                                  | Gap                                                                                        | Priority                                                           | Why                                                                                                                                                                                                                                                               |
| --------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------ | ------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Token semantic layer for Smile Savers               | CSS custom properties, named semantically, no metadata                                                                                                | Full RAW→ATOMIC→PRIMITIVE→SEMANTIC→COMPONENT→COMPOSITION model with id/status/consumers | Real, buildable — the values exist, only structure is missing                              | **P1**                                                             | Directly reusable pattern from the Labs pipeline; would not require fabricating content                                                                                                                                                                           |
| Component contracts                                 | 5/47 have typed Props; 0/47 have documented states/slots/anatomy                                                                                      | Full component records per §4                                                           | 42/47 components have literally no documented contract to extract                          | **P2 at best, mostly unbuildable without fabrication**             | Extracting a Props type from existing `.astro` files (where present) is evidence-based; inventing anatomy/slots/states for the other 42 is not                                                                                                                    |
| Pattern/anti-pattern taxonomy                       | None                                                                                                                                                  | Full pattern records (§22-23)                                                           | 100% gap, zero source evidence                                                             | **Not buildable from evidence**                                    | No pattern library, style guide, or design documentation exists anywhere in this repo to extract from                                                                                                                                                             |
| Design intent/rationale                             | None                                                                                                                                                  | "Why it exists / when not to use it" per component (§21)                                | 100% gap                                                                                   | **Not buildable from evidence**                                    | Would require inventing rationale never recorded by the original site authors — directly the fabrication §31 forbids                                                                                                                                              |
| Change-impact engine                                | None                                                                                                                                                  | Full forward/reverse impact graph (§18)                                                 | Real, buildable once a component/token graph exists                                        | **P1** (depends on the token/component graph above existing first) | Legitimate, valuable, and mechanically derivable from a real dependency graph rather than requiring invented content                                                                                                                                              |
| Evidence invalidation                               | None                                                                                                                                                  | `INVALIDATES`/`INVALIDATED_BY` edges (§19)                                              | Buildable as a consequence of the impact engine                                            | **P2**                                                             | Depends on P1 items existing first                                                                                                                                                                                                                                |
| Agent-neutral core + Codex/OpenCode/Cursor adapters | None; only Claude Code touches this repo                                                                                                              | Full multi-agent adapter architecture (§26)                                             | Speculative infrastructure for tools with zero presence in this session or repo            | **Declined**                                                       | No evidence any other agent will ever touch this repository; building adapters for tools not in use serves no measured need, and the request's own §2 rule ("do not add a node/edge unless it improves an actual agent task") applies equally to whole subsystems |
| Accessibility-to-component linkage                  | Stage 16 produced route-level WCAG findings (color-contrast, missing label, invalid ARIA — see prior commits) but not linked to individual components | Full requirement→component→state→test linkage (§15)                                     | Partially buildable: the 6 known violations could be traced to their originating component | **P2**                                                             | Real, evidence-backed, bounded scope — unlike inventing a full accessibility taxonomy for components with no known issues                                                                                                                                         |

## Recommendation

Two things are genuinely justified by evidence and worth building next, both **P1**, both mechanically derivable without fabricating content:

1. **A real semantic token model for Smile Savers' own `global.css`** — mirroring the Labs pipeline's token-layer discipline, but pointing at this repo's actual CSS custom properties (with `id`, `layer`, `consumers` computed by grepping `var(--x)` usage across `src/`).
2. **A change-impact engine** built on top of (1) — "what consumes `--color-accent`" is a real, answerable grep-and-graph question today.

Everything else in the 33-section request — component anatomy, patterns, anti-patterns, design intent, multi-agent adapters — requires either fabricating content this repository has never recorded, or building infrastructure for agents that have never touched this repository. Per this project's evidence-first discipline (already tested across 20+ stages this session), that is not implemented here without further direction, per the request's own instruction to stop at the implementation boundary.

## P1 close-out addendum

Both items this audit flagged as P1 are now built — see `design-intelligence/scripts/token-inventory.mjs`, `token-lineage.json`, `cascade-map.json`, `change-impact.mjs`, `evidence-invalidation.mjs`, `agent-context.mjs`, `semantic-validate.mjs`, and `design-intelligence/reports/release-gate.json` for the P1-scoped gate. Real evidence produced: 136 tokens inventoried from `src/styles/global.css` (line-cited), 55 confirmed orphans (checked against both literal `var()` usage and Tailwind v4 utility-class usage — not just the former), 2 genuine media-scoped token redeclarations (`--font-size-5xl`@1440px, `--container-xl`@1920px) captured as `VARIES_BY` edges, the DaisyUI `!important` brand-lock block captured as 8 `OVERRIDDEN_BY` edges, 5 `ALIASES` edges, 880 `CONSUMED_BY` edges. `semantic-validate.mjs` ran all 13 §T checks against this real data: 12 PASS, 1 correctly `NOT_APPLICABLE` (no semantic-promotion mechanism exists to test).

The "not buildable / declined" rows in the table above are unchanged by P1 — component anatomy, patterns, anti-patterns, design intent, and multi-agent adapters still have zero source evidence to build from and remain out of scope.
